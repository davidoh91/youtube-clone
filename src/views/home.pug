extends base
include mixins/video

block content
    h2 Welcome, here are the trending videos
    each video in videos
        //+ << imports the function from mixin template
        +video(video)
    else
        li Sorry, nothing was found
