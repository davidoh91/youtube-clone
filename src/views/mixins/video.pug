mixin video(video)
    //Mixins is smart partials (html templates that deals with request data and also serves different types of data)
    div
        h4
            a(href=`/videos/${video.id}`)=video.title
        p=video.description
        ul
            each hashtag in video.hashtags
                li=hashtag 
        small=video.createdAt
        hr