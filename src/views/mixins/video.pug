mixin video(video)
    //Mixins is smart partials (html templates that serves differnt data)
    div
        h4
            a(href=`/videos/${video.id}`)=video.title
        ul
            li #{video.rating}/5
            li #{video.comments} comments
            li Posted #{video.createdAt}
            li #{video.views} views
            